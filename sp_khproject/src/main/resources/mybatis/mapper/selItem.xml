<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="selItemMapper">
	<select id="getNextSeq" resultType="_int">
		SELECT SEL_ITEM_SEQ.NEXTVAL FROM DUAL
	</select>
<!-- <insert id="insertData" parameterType="boardDto">
		INSERT INTO SEL_ITEM(SEL_ID, SEL_SELNAME, SEL_INTEREST, SEL_LOCATION)
	</insert> -->
	<!-- 오름차순으로 정렬 -->
	<select id="getData" resultType="selItemDto">
		SELECT * FROM SEL_ITEM ORDER BY SEL_ID DESC
	</select>
	
	<select id="selectData" resultType="selItemDto">
		SELECT * FROM SEL_ITEM WHERE SEL_INTEREST = #{selectData} ORDER BY SEL_ID DESC
	</select>

	<insert id="insertData" parameterType="selItemDto">
		INSERT INTO SEL_ITEM(SEL_ID, SEL_NAME, SEL_INTEREST, SEL_LOCATION, SEL_TITLE, SEL_CONTENT) 
	VALUES (SEL_ITEM_SEQ.NEXTVAL,#{sel_name},#{sel_interest}, #{sel_location}, 'test', #{sel_content})
	</insert>
	
	<select id="searchData" resultType="selItemDto">
		SELECT * FROM SEL_ITEM WHERE SEL_INTEREST LIKE '%'||#{search}||'%' ORDER BY SEL_ID DESC
	</select>
	


</mapper>